model:
  checkpoint_dir: ../outputs/qwen25-banking-lora
  max_seq_length: 512
  dtype: null
  load_in_4bit: true

data:
  label_mapping_path: ../configs/label_mapping.json
  test_csv: ../sample_data/test.csv

inference:
  max_new_tokens: 16
  do_sample: false

output:
  prediction_csv: ../outputs/eval_predictions.csv
